Steps to run discovery server application

```
cd discovery-server
mvn clean install
```

```java -jar target/discovery-server-0.0.1-SNAPSHOT.jar```

Registered services can be seen at below URL

```http://localhost:9761/```
